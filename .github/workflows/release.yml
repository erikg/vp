name: release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  dist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libsdl2-dev libsdl2-image-dev libssl-dev \
            pkg-config autoconf automake
      - name: dist tarball
        run: |
          autoreconf -fi
          ./configure
          make dist
      - name: test dist tarball
        run: |
          mkdir dist-check
          tar xzf vp-*.tar.gz -C dist-check --strip-components=1
          cd dist-check
          ./configure
          make
          sh tests/net-tests.sh src/vp
      - name: create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" vp-*.tar.gz \
            --title "vp ${GITHUB_REF_NAME#v}" --generate-notes
